apiVersion: v1
kind: Service
metadata:
  name: dashboard
spec:
  selector:
    app: dashboardapp
    tier: frontend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  type: LoadBalancer
